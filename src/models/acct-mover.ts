export class AcctMover {

  id: string;
  sourceClass: string;
  displayText: string;

  constructor(id: string, sourceClass: string, displayText: string) {
    this.id = id;
    this.sourceClass = sourceClass;
    this.displayText = displayText;
  }
}
